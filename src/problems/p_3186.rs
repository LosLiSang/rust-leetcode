pub struct Solution;

impl Solution {
    /// 最大的damage, 至少要合并power值相同的数值
    /// 建立两个数组total_damage和count
    /// 用于动态规划的dp(假设需要)
    /// 问题1. 需要排序吗
    /// 问题2. 
    
    pub fn maximum_total_damage(power: Vec<i32>) -> i64 {
        1    
    }
}